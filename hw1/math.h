#pragma once

#include <cstddef>
#include <vector>

namespace hw1 {

namespace math {

double FunctionAt(double x);

std::size_t hash(std::vector<std::size_t> vec);

} // namespace math

} // namespace hw1
